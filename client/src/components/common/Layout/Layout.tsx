import React, { FC } from 'react';
import s from './Layout.module.css';

const Layout: FC = () => {
  return (
    <div className={s.root}>
      <h1>testing 123</h1>
    </div>
  );
};

export default Layout;
